<%= form_with(model: base_point, local: true) do |form| %>
  <% if base_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(base_point.errors.count, "error") %> prohibited this base_point from being saved:</h2>

      <ul>
      <% base_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :base_point_name %>
  </div>

  <div class="field">
    <%= form.label :attendance_state %>
    <%= form.number_field :attendance_state, id: :base_point_attendance_state %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number, id: :base_point_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
